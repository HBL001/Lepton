# No object library — this builds a standalone tool
# The actual executable is defined in the top-level CMakeLists.txt
# You could still use OBJECT libraries here if you split up tools

# This file is optional but keeps the directory self-contained
